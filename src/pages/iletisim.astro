---
import BaseLayout from "../layouts/BaseLayout.astro";

const title = "İletişim | Fizyoterapist Ezgi Acem";
---

<BaseLayout
  title={title}
  description="Randevu ve sorularınız için iletişime geçin."
>
  <div
    class="relative min-h-[calc(100vh-64px)] pt-36 pb-20 overflow-hidden md:bg-gradient-to-b md:from-orange-50/50 md:to-white bg-[#FAFAFA]"
  >
    <!-- Decorative background elements -->
    <div
      class="absolute top-0 left-0 w-full h-full overflow-hidden -z-10 pointer-events-none hidden md:block"
    >
      <div
        class="absolute top-[-10%] right-[-5%] w-96 h-96 bg-yellow-200/40 rounded-full blur-3xl animate-blob"
      >
      </div>
      <div
        class="absolute bottom-[-10%] left-[-5%] w-96 h-96 bg-blue-200/40 rounded-full blur-3xl animate-blob animation-delay-2000"
      >
      </div>
      <div
        class="absolute top-[40%] right-[20%] w-64 h-64 bg-green-200/30 rounded-full blur-3xl animate-blob animation-delay-4000"
      >
      </div>
    </div>

    <div class="container mx-auto px-4 max-w-5xl relative z-10">
      <header class="text-center mb-12">
        <h1 class="text-4xl md:text-5xl font-bold text-[#FF8A65] mb-4">
          İletişime Geçin
        </h1>
        <p class="text-lg text-slate-500 max-w-2xl mx-auto">
          Sorularınız, randevu talepleriniz veya çocuğunuz hakkında danışmak
          istedikleriniz için buradayız.
        </p>
      </header>

      <div
        class="bg-white rounded-[3rem] shadow-xl overflow-hidden flex flex-col md:flex-row border border-slate-100"
      >
        <!-- Contact Info Side -->
        <div
          class="bg-gradient-to-br from-orange-500 via-emerald-600 to-teal-600 text-white p-12 md:w-5/12 flex flex-col justify-between relative overflow-hidden"
        >
          <div
            class="absolute top-0 right-0 w-64 h-64 bg-white/10 rounded-full blur-3xl -mr-16 -mt-16"
          >
          </div>
          <div
            class="absolute bottom-0 left-0 w-48 h-48 bg-white/10 rounded-full blur-2xl -ml-12 -mb-12"
          >
          </div>

          <div class="relative z-10">
            <h3 class="text-2xl font-bold mb-8">İletişim Bilgileri</h3>

            <div class="space-y-8">
              <div class="flex items-start gap-4 group">
                <div
                  class="w-12 h-12 bg-white/20 rounded-2xl flex items-center justify-center text-2xl group-hover:scale-110 transition-transform duration-300 backdrop-blur-sm"
                >
                  📍
                </div>
                <div>
                  <h4 class="font-bold text-lg mb-1">Adres</h4>
                  <p class="text-white/90 leading-relaxed">
                    Adana, Türkiye
                    <br /><span class="text-sm opacity-75"
                      >(Detaylı adres randevu sonrası iletilir)</span
                    >
                  </p>
                </div>
              </div>

              <div class="flex items-start gap-4 group">
                <div
                  class="w-12 h-12 bg-white/20 rounded-2xl flex items-center justify-center text-2xl group-hover:scale-110 transition-transform duration-300 backdrop-blur-sm"
                >
                  📧
                </div>
                <div>
                  <h4 class="font-bold text-lg mb-1">E-posta</h4>
                  <a
                    href="mailto:iletisim@fztezgiacem.com"
                    class="text-white/90 hover:text-white hover:underline transition-colors"
                  >
                    <p class="text-white font-medium">
                      <!--email_off-->
                      iletisim@fztezgiacem.com
                      <!--/email_off-->
                    </p>
                  </a>
                </div>
              </div>

              <div class="flex items-start gap-4 group">
                <div
                  class="w-12 h-12 bg-white/20 rounded-2xl flex items-center justify-center text-2xl group-hover:scale-110 transition-transform duration-300 backdrop-blur-sm"
                >
                  📞
                </div>
                <div>
                  <h4 class="font-bold text-lg mb-1">Telefon</h4>
                  <p class="text-white/90">
                    (Randevu için lütfen e-posta veya form üzerinden ulaşınız)
                  </p>
                </div>
              </div>
            </div>
          </div>

          <div class="relative z-10 mt-12">
            <p class="text-white/80 text-sm">
              Mesai saatleri dışında gönderilen mesajlara en kısa sürede dönüş
              yapılacaktır.
            </p>
          </div>
        </div>

        <!-- Form Side -->
        <div class="p-12 md:w-7/12 bg-white">
          <form id="contactForm" class="space-y-6">
            <div>
              <label
                for="name"
                class="block text-sm font-bold text-slate-600 mb-2 ml-1"
                >Ad Soyad</label
              >
              <input
                type="text"
                id="name"
                name="name"
                required
                class="w-full px-6 py-4 bg-slate-50 border border-slate-200 rounded-2xl focus:ring-4 focus:ring-orange-100 focus:border-[#FF8A65] outline-none transition-all placeholder:text-slate-400"
                placeholder="Adınız Soyadınız"
              />
            </div>

            <div>
              <label
                for="email"
                class="block text-sm font-bold text-slate-600 mb-2 ml-1"
                >E-posta</label
              >
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-6 py-4 bg-slate-50 border border-slate-200 rounded-2xl focus:ring-4 focus:ring-orange-100 focus:border-[#FF8A65] outline-none transition-all placeholder:text-slate-400"
                placeholder="ornek@email.com"
              />
            </div>

            <div>
              <label
                for="message"
                class="block text-sm font-bold text-slate-600 mb-2 ml-1"
                >Mesajınız</label
              >
              <textarea
                id="message"
                name="message"
                rows="4"
                required
                class="w-full px-6 py-4 bg-slate-50 border border-slate-200 rounded-2xl focus:ring-4 focus:ring-orange-100 focus:border-[#FF8A65] outline-none transition-all placeholder:text-slate-400 resize-none"
                placeholder="Mesajınızı buraya yazın..."></textarea>
            </div>

            <button
              type="submit"
              class="w-full bg-[#FF8A65] text-white font-bold py-4 rounded-2xl hover:bg-[#FF7043] transition-all transform hover:-translate-y-1 shadow-lg shadow-orange-200 hover:shadow-orange-300"
            >
              Gönder
            </button>
            <p id="formStatus" class="text-center text-sm mt-4 hidden"></p>
          </form>
        </div>
      </div>
    </div>
  </div>

  <script>
    import PocketBase from "pocketbase";

    const pb = new PocketBase("/");
    const form = document.getElementById("contactForm") as HTMLFormElement;
    const status = document.getElementById("formStatus");

    if (form) {
      form.addEventListener("submit", async (e) => {
        e.preventDefault();
        const formData = new FormData(form);
        const data = {
          name: formData.get("name"),
          email: formData.get("email"),
          message: formData.get("message"),
        };

        if (status) {
          status.textContent = "Gönderiliyor...";
          status.className = "text-center text-sm mt-4 text-slate-500 block";
        }

        try {
          await pb.collection("messages").create(data);

          if (status) {
            status.textContent = "Mesajınız başarıyla iletildi! Teşekkürler.";
            status.className =
              "text-center text-sm mt-4 text-green-500 block font-bold";
          }
          form.reset();
        } catch (err) {
          console.error(err);
          if (status) {
            status.textContent =
              "Bir hata oluştu. Lütfen daha sonra tekrar deneyin veya e-posta gönderin.";
            status.className = "text-center text-sm mt-4 text-red-500 block";
          }
        }
      });
    }
  </script>
</BaseLayout>
